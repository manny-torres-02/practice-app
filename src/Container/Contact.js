import React from 'react'

const contacts =(props) => {

    return (
      <div>
        <p>I am {this.contact.name} and I am {this.contact.age}</p>
      </div>
    )

  
}

export default contacts;
